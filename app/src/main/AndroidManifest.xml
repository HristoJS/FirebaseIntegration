<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evilcorp.firebaseintegration">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <permission
        android:name="com.evilcorp.firebaseintegration.db.AddressContentProvider.permRead"
        android:description="@string/permission_desc"
        android:label="@string/permission_label"
        android:protectionLevel="signature" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="5d808aa660e3831ecdcc012117efdff70b8977a6" />

        <activity android:name=".main.MainActivity"
            android:theme="@style/AppTheme"/>

        <service
            android:name=".services.MyFirebaseInstanceIDService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.loginInteractor.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            >
            <intent-filter>
                <action android:name="com.google.loginInteractor.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".login.LoginActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.GeoService"
            android:enabled="true"
            android:exported="false" />

        <provider
            android:name=".db.AddressContentProvider"
            android:authorities="com.evilcorp.firebaseintegration.db.AddressContentProvider"
            android:exported="true"
            android:readPermission="com.evilcorp.firebaseintegration.db.AddressContentProvider.permRead"
            android:writePermission="disabled" />

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"/>
    </application>

</manifest>